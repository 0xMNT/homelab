apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: app-1db-access
  namespace: app1-pg-cluster
spec:
  action: ALLOW
  rules:
  - to:
    - operation:
        ports: ["5432"]
