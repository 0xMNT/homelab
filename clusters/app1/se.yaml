apiVersion: networking.istio.io/v1beta1
kind: ServiceEntry
metadata:
  name: app1-postgres-service-entry
  namespace: app1-pg-cluster
spec:
  hosts:
  - app1-pg-cluster.local
  ports:
  - number: 5432
    name: tcp-postgres
    protocol: TCP
  location: MESH_INTERNAL
  resolution: DNS
